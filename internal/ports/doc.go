// internal/ports/doc.go
// Documents the shared router port boundary for policycheck and wrapper features.
// Keeps the package overview aligned with enforced file header requirements.
// Package ports Defines the router port contracts for the policy engine.
//
// Package Concerns:
// - Defining the walk, scanner, config, and read-file provider interfaces.
// - Defining the CLI wrapper subsystem port boundary (CLIWrapperDispatcher, CLIWrapperSecurityGate, CLIWrapperMacroRunner, CLIWrapperFormatter).
package ports
