// Package types Defines the shared data types used throughout the policycheck engine.
// These types form the contract between policy checks, the CLI layer, and the scanner adapters.
//
// Package Concerns:
// - Define shared violation, finding, fact, and result types for the policy engine.
// - Provide value-bearing types with no external dependencies beyond stdlib.
package types
