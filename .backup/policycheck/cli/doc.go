// Package cli Implements the command-line interface for the policycheck tool.
// It handles argument parsing, output formatting, and exit code determination.
//
// Package Concerns:
// - Parse CLI flags and orchestrate policy checks via the core engine.
// - Print results in text, JSON, or NDJSON format and determine exit code.
package cli
